"use client";
import { motion } from "motion/react";
import NavLinks from "./NavLinks";

const MainNav = () => {
  return (
    <nav className="  ">
      <NavLinks />
    </nav>
  );
};

export default MainNav;
